package dailyKill.integerBreak;

public class Solution {
    public int integerBreak(int n) {
        //if (n <= 3) {return n - 1;}
        //int mod = n % 3;
        //int div = n / 3;
        //return switch (mod) {
        //    case 0 -> (int)Math.pow(3, div);
        //    case 1 -> (int)Math.pow(3, div - 1) * 4;
        //    case 2 -> (int)Math.pow(3, div) * 2;
        //    default -> 0;
        //};
        return 0;
    }
}